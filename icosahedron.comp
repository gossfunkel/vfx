#version 430

const float TAU = 6.2831853;

layout (local_size_x = 3, local_size_y = 4) in;
//uniform float osg_FrameTime;
uniform uint num_verts;

layout (std430, binding = 0) buffer vert_buff {
    vec3 pos[];
};

void main() {
    uint vtx_id = 4 * gl_GlobalInvocationID.x + gl_GlobalInvocationID.y;
}
